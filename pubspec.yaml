name: factorylink
description: "Factory Direct. Group Smart. Save Big."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  shared_preferences: ^2.3.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  syncfusion_flutter_pdf: ^32.2.7

flutter:
  uses-material-design: true
